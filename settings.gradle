rootProject.name = "CVSDK OCR"
include ":ocr-older-sdk" //first cv experiments, images acquired via camera only through multiple shots
include ":ocr-videostream" //current cv sdk for ocr, images acquired via camera through video stream, mimics barcode videostream.
